% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{zscore_within_cohort}
\alias{zscore_within_cohort}
\title{Z-score Normalize Within Cohort by Timepoint}
\usage{
zscore_within_cohort(
  data,
  by = "timepoint",
  markers = c("ALB", "TC", "LYM", "CRP", "SMI", "SFI", "VFI")
)
}
\arguments{
\item{data}{A data.frame with id, timepoint, and 7 markers.}

\item{by}{Character. Column to group by. Default "timepoint".}

\item{markers}{Character vector. Marker names to standardize.
Default \code{c("ALB", "TC", "LYM", "CRP", "SMI", "SFI", "VFI")}.}
}
\value{
A data.frame with original columns + 7 z-scored columns
  (suffix \code{_z}).
}
\description{
Standardizes 7 markers to z-scores within the cohort, separately
for each timepoint. This ensures comparability across timepoints
and centers the data for the LCTM.
}
