## ----setup, include = FALSE---------------------------------------------------
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
library(quick.lctm.escc)

## ----eval=FALSE---------------------------------------------------------------
# data(example_lctm)
# fit <- fit_lctm_full(example_lctm, K = 2, n_iter = 1000, seed = 42)
# head(fit@posterior)
# table(fit@cluster)

